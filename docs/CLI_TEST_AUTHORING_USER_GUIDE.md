# CLI Test Authoring User Guide

## Purpose

Purpose: keep the legacy CLI-oriented entry point while redirecting authors to the canonical DSL guide.

The canonical guide for writing, importing, validating, and running Robot Test DSL source is:

- [USER_GUIDE_ROBOT_TEST_DSL.md](./USER_GUIDE_ROBOT_TEST_DSL.md)

Use that document for:

- DSL syntax
- language features and semantics
- UI and CLI authoring workflow
- import/export/validate commands
- practical examples

This file remains only as a compatibility pointer for older references.
