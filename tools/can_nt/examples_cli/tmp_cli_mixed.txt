# Good: enter config, define device, enter device mode, set fields
configure terminal
device leftMotor
set vendor rev
set deviceType MotorController
set deviceId 5
exit

device rightMotor
set vendor rev
set deviceType MotorController
set deviceId 4
exit

# Good: group config
group drive
add device leftMotor
add device rightMotor
bind trigger analog
show groups --json
show devices --json
end

# Bad: missing show target
show

# Bad: bind in exec mode
bind

# Good: show status in exec mode
show status
exit
