# Robot 2026 bridgeConfig recreation (from 2186_2026_robot.json)
configure terminal

# Initial empty state
show devices --json
show groups --json

# Devices
device "Encoder (CANCoder) (swerve-back-left)"
exit

device "Encoder (CANCoder) (swerve-back-right)"
exit

device "Encoder (CANCoder) (swerve-front-left)"
exit

device "Encoder (CANCoder) (swerve-front-right)"
exit

device "IMU (Pigeon2)"
exit

device "Climb Motor"
exit

device "Fuel Intake Motor"
exit

device "Pivot Intake Motor"
exit

device "Pivot Intake Follower"
exit

device "Feeder Motor"
exit

device "Flywheel Motor (Leader)"
exit

device "Flywheel Motor (Follower)"
exit

device "Drive Motor (swerve-back-left)"
exit

device "Angle Motor (swerve-back-left)"
exit

device "Drive Motor (swerve-back-right)"
exit

device "Angle Motor (swerve-back-right)"
exit

device "Drive Motor (swerve-front-left)"
exit

device "Angle Motor (swerve-front-left)"
exit

device "Drive Motor (swerve-front-right)"
exit

device "Angle Motor (swerve-front-right)"
exit

# After devices
show devices --json

# Groups
group swerve_front_left
member assign "Drive Motor (swerve-front-left)"
member assign "Angle Motor (swerve-front-left)"
member assign "Encoder (CANCoder) (swerve-front-left)"
exit

group swerve_front_right
member assign "Drive Motor (swerve-front-right)"
member assign "Angle Motor (swerve-front-right)"
member assign "Encoder (CANCoder) (swerve-front-right)"
exit

group swerve_back_left
member assign "Drive Motor (swerve-back-left)"
member assign "Angle Motor (swerve-back-left)"
member assign "Encoder (CANCoder) (swerve-back-left)"
exit

group swerve_back_right
member assign "Drive Motor (swerve-back-right)"
member assign "Angle Motor (swerve-back-right)"
member assign "Encoder (CANCoder) (swerve-back-right)"
exit

# After groups
show groups --json
show groups
show bindings --json
show selected-device
show devices --json

# Export a CLI script snapshot
export cli-script tools/can_nt/out_cli_robot2026.txt

end

# Exec mode output
show status
exit

